const mocha = require('mocha');
const chai = require('chai');
const chaiHTTP = require('chai-http');

const isQuote = require('../isQuote');

describe('isQuote', function() {
    it('should give right answers for equal and unequal inputs');
  }
);